# Copied into apps/api at runtime; every value has a sane default.
KOBRALINK_DATA_DIR=./data
KOBRALINK_PORT=7100
BETTER_AUTH_URL=http://localhost:7100
# BETTER_AUTH_SECRET=   # généré dans <data>/.auth-secret si absent
